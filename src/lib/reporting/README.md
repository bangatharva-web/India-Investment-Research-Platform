# Report generation

This folder is for AI prompt orchestration, structured report validation, and Word/PDF export logic.

Important: AI calls must happen on the backend only. Do not expose OpenAI/Claude keys in frontend code.
